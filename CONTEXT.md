# CONTEXT — 领域术语表

本文件只是术语表，不含实现细节。

## 攻略（Guide）

一次旅行规划的最终产物：静态 HTML 页面及衍生格式（.ics、小红书图文、PDF）。由宿主 AI 编排生成，围绕**攻略 schema** 单缝组织。

## 攻略 schema（Guide Schema）

攻略的唯一数据缝。build 阶段组装，render/validate/export 消费。所有能力（地图、12306 车次、天气、预算、jev 评分）都通过 schema 字段进出，不设第二数据通道。

## 宿主 AI（Host AI）

运行本 skill 的 AI agent。负责编排生成流程、生成行程内容、复核告警。skill 本身不含 LLM API 调用。

## 判断点（Judgment Point）

流程中一个可插入 jev 外部判断或启发式判断的决策位。每个判断点有独立开关，默认关闭（per-point opt-in）。当前定义两个：素材可信度、行程合理性。

## 素材可信度（Material Credibility）

对采集到的 untrusted 素材（小红书等社交平台内容）的可信程度评分，判断哪些素材可采纳为攻略依据。jev 判断点之一。

## 行程合理性（Itinerary Plausibility）

对单日行程的语义级合理程度判断：强度、动线、节奏。区别于硬规则——硬规则管「物理可行」，行程合理性管「体验上靠谱」。jev 判断点之一。

## 硬规则（Hard Rules）

规则引擎执行的确定性校验：营业时间冲突、衔接时间不足、跨城不合理、预算超支。唯一拥有 blocking 权力的校验层。

## Blocking / Advisory

告警分级。blocking：未清空则产物加水印「未校验通过」，宿主 AI 必须修订直至清空。advisory：提示性告警，宿主 AI 酌情复核，不阻塞产物。

## 启发式孪生（Heuristic Twin）

jev 判断点在不可用状态（无 key / 判断点未开启 / 调用失败 / 超限）下的退路：退回宿主 AI 自行评估。降级必须显式，产物不因 jev 缺失而失败。

## Confidence 标记

产物页面上的数据来源可信度视觉标记：实时数据 / 校验通过 / 估算数据。与告警分级是两个维度：confidence 描述数据来源，告警描述校验结果。

## 目的地文化属性（Destination Identity）

目的地的文化身份决定攻略页面的视觉设计语言。宿主 AI 从设计库（awesome-design-md）的 73 个品牌设计系统中**严格选择**气质最贴近的设计——库外不发明设计。渲染端按所选品牌的设计 token 渲染。设计决策见 spec #16。

## 页面版面（Layout）

攻略页面的交互结构：tab 分区（总览地图 / 各天 / 预算 / 告警），非单一长条滚动。行程点可展开详情面板（介绍/营业时间/门票/理由）。

## 素材图片（Spot Image）

从 untrusted 素材中提取的图片，写入攻略 schema 的 spot.image 字段，渲染时直出并带来源标记。无图时降级为文化主题占位图。

## 交通线（Transit Leg）

跨城交通段（如高铁线）在地图上的呈现。「全程」视图 = 跨城概览（高铁线 + 两端车站与市内动线同图，视野拉到跨城）；「Day N」视图 = 市内动线；「交通」视图 = 深化版（车次/时刻/票价/导航）。经攻略 schema 的 routes 字段进出。

## 时间窗（Time Window）

行程点在地图与时间轴上的「几点到几点」标注。地图 marker 直接带时间窗标签（可切换关闭），详情面板与时间轴同步呈现。经攻略 schema 的 spot 字段进出。
